import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'vue_binary_tree',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
